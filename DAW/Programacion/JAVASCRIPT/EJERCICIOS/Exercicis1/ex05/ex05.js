"use strict";

let base, altura, radi;

base = 15
altura = 22
radi = 12

alert("La hipotenusa del triangle es :" + (Math.sqrt(Math.pow(base, 2) + Math.pow(altura, 2))))

alert("La circumferencia d'un cercle es:" + (2 * (radi * Math.PI)))